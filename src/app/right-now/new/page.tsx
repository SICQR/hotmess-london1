import { RightNowCreateForm } from '@/components/rightnow/RightNowCreateForm';

export const metadata = {
  title: 'Post RIGHT NOW — HOTMESS LONDON',
  description: 'Drop what you\'re into for the next 60 minutes and see who answers.',
};

export default function RightNowCreatePage() {
  return <RightNowCreateForm />;
}
